# Step 10.2 to 10.3 Handoff

## Completed boundary

Milestone K Step 10.2 packages and locally verifies the existing read-only
FastAPI backend without deploying it. The completed boundary provides:

- an exact Python 3.14.7 slim-trixie base-image digest;
- a deny-by-default build context containing only project metadata and source;
- exact Uvicorn 0.53.0 dependency and process configuration;
- a non-root UID/GID 10001 runtime with a narrowly writable artifact parent;
- a fail-closed entry point that accepts only `PLP_ENVIRONMENT=production`;
- one Uvicorn worker for the process-local rate-limit contract;
- disabled proxy-header trust until a real deployment boundary is approved;
- process liveness as the image healthcheck and dependency readiness as a
  separate HTTP 503 gate; and
- deterministic static, subprocess and built-container verification.

No credential, database URL, raw data, registry history or model artifact is
present in the image.

## Preserved contracts

- Development and test PostgreSQL remain explicitly isolated. Production has
  no fallback and reports `production_database_not_configured` when unconfigured.
- The actual registry remains `development_accepted` with no active model. The
  empty image does not synthesize a registry and readiness reports
  `no_active_model`.
- All eleven historical raw-manifest checks remain part of the local release
  gate; no raw or artifact corpus is copied into the image.
- The FastAPI surface remains exactly sixteen GET-only operations with the
  existing request-ID, error-envelope, pagination, OpenAPI, CORS, security-
  header and rate-limit behavior.
- The sealed 2025–26 targets remain uninspected and no final-test evidence was
  created.
- No provider was selected or contacted. No prediction, scoreline distribution
  or simulation was generated. No registry entry was mutated.
- Imports remain side-effect free and external dependencies remain lazy.
- No scheduling, recurring monitor, automation, CI/CD, GitHub Actions, hosted
  infrastructure or deployment configuration was added.

## Verification

The complete local release suite remains:

```powershell
ruff check .
ruff format --check .
mypy src tests migrations
python -m pip check
pytest --cov --require-local-release
```

The image is additionally built as `pl-platform-backend:local`, run only on a
loopback port and checked for non-root identity, healthy liveness, fail-closed
readiness, production security headers, request-ID preservation and the exact
sixteen-operation OpenAPI surface. A non-production environment override is
required to exit before serving.

Final results:

- Python 3.14.7: 653 tests passed with no skips;
- branch coverage: 91.07%, above the required 90%;
- Ruff lint and formatting: passed across 279 files;
- strict mypy: passed across 229 source, test and migration Python files;
- dependency consistency: passed;
- both isolated PostgreSQL 18.4 targets remained at exact head
  `f0009_step_7_9` after the rollback-only full migration cycle;
- all eleven historical captures passed exact manifest verification; and
- the actual registry remained `development_accepted` with zero active models.

## Next boundary

Step 10.3 is the exact next roadmap item, but it is not approved or started by
this handoff. Its scope is provisioning and migrating a hosted PostgreSQL
database while preserving least privilege, explicit production configuration
and the reviewed nine-revision Alembic chain. It must not deploy FastAPI (Step
10.4), select a current-data provider, import the production artifact corpus,
inspect sealed targets, mutate registry state or add unapproved automation.

## Approved continuation status

The user subsequently approved Steps 10.3 and 10.4 and selected Neon Free for
PostgreSQL plus Render Free for the Docker API, with no payment details. The
local continuation now provides:

- an explicit TLS-only production runtime target and a separate direct
  production migration setting;
- pooled unprivileged `pl_api` reads with manual owner-only migrations;
- production readiness and resource-query selection without local fallback;
- one manual Render Free Singapore service with auto-deploy disabled;
- dynamic platform-port handling and `/health/live` deployment health checks;
- Render-specific validated `CF-Connecting-IP` rate-limit identity without
  trusting generic proxy headers; and
- the complete manual provisioning, grant, migration and verification runbook.

The complete local release suite passes 666 tests at 91.10% branch coverage.
Step 10.3 subsequently completed on Neon Free: PostgreSQL 18.6 is migrated to
`f0009_step_7_9` and `pl_api` has SELECT on all 111 tables with no write or
elevated role capability. Render is authenticated but Step 10.4 remains open
because the repository has no reviewed commit or published source. No corpus
import, registry mutation or Step 10.5 work has occurred.
