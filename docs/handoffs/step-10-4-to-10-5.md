# Step 10.4 to 10.5 Handoff

## Completed release boundary

Steps 10.1 through 10.4 are complete. The local release gate, production
container, Neon Free database and Render Free web service have been implemented
and verified without adding a paid resource, automation, scheduling or CI/CD.

The deployed source is commit
`461f31f637ad9bbe52eb07917bf5771606ee5c54`. Render runs one manually deployed
Free Docker web service in Singapore at
`https://premier-league-prediction-api.onrender.com`; automatic deploys are off
and `/health/live` is the platform health check. Neon runs PostgreSQL 18.6 in
Singapore with the exact migration head `f0009_step_7_9`. The pooled `pl_api`
role can select all 111 migrated tables, cannot write any table and has no
elevated role capability.

## Acceptance evidence

- Python 3.14.7: 666 tests passed with no skips and 91.10% branch coverage.
- Ruff lint and formatting, strict mypy over 230 Python files and dependency
  consistency passed.
- All 11 historical raw captures retain their manifest verification.
- Public liveness returns HTTP 200.
- Public readiness intentionally returns HTTP 503 with PostgreSQL `ready` and
  active model `no_active_model`.
- Public OpenAPI contains exactly sixteen paths and sixteen GET operations,
  with no mutating operation.
- Request-ID echo, uniform error envelopes, offset pagination, CORS denial,
  production security headers and rate-limit headers were verified.
- Render retains exactly seven reviewed environment variables. Obsolete
  duplicate production-database entries were removed and only the pooled
  read-only runtime URL was sent to Render.

## Preserved state

The actual registry remains `development_accepted`; no active model exists.
No production corpus or artifact was imported, no provider was selected or
contacted, no prediction or scoreline distribution was generated, no
simulation was run and the sealed 2025–26 target boundary was not inspected.
The direct migration credential was not sent to Render. Imports remain
side-effect free and external dependencies remain lazy.

## Handoff outcome

Steps 10.5 and 10.6 were subsequently approved and implemented locally. They
added rollback-only 2024–25 historical-to-API validation, migration
`f0010_step_10_5`, exact secret/quota checks and transient dependency recovery
without violating this handoff's preserved boundaries. Continue with the
[Step 10.6 to 10.7 handoff](step-10-6-to-10-7.md).
