# Step 10.6 to 10.7 Handoff

## Completed implementation boundary

Steps 10.5 and 10.6 are implemented locally. The release gate now projects the
real 2024–25 historical corpus through PostgreSQL and the FastAPI transport
inside one rollback-only transaction. It proves the reviewed season has 20
members and 380 finished fixtures, pagination and filtering are exact,
prediction and simulation collections remain empty, repeated response bodies
are identical and neither database state nor artifact bytes persistently
change.

The validation found an original migration defect: the canonical-dataset
deferred validator referenced the superseded `is_complete` column. The new
linear revision `f0010_step_10_5` replaces that function with the final
`completed` column contract and changes no data, table or privilege. Local
development and test databases are at the new head.

Step 10.6 additionally pins the exact seven-variable Render environment,
deny-by-default secret exclusions, a two-connection pool plus one overflow,
provider and HTTP quota boundaries, sanitized transient database failure and
next-request recovery, existing workflow recovery and byte-identical replay.

The final local release gate passes on Python 3.14.7: 669 tests with no skips,
91.12% branch coverage, Ruff lint and formatting across 287 files, strict mypy
across 232 source/test/migration files and dependency consistency. The complete
rollback-only migration cycle covers all ten revisions through
`f0010_step_10_5`.

## Preserved state

- Hosted Neon and Render remain on published commit `461f31f` and migration
  head `f0009_step_7_9`; no uncommitted migration was applied externally.
- The actual registry is still `development_accepted`; no active model exists.
- No production artifact corpus was imported and no provider was selected or
  contacted.
- No prediction, scoreline distribution or simulation was generated.
- The sealed 2025–26 target values were not read and no final-test evidence was
  created.
- No automation, scheduling, recurring monitor, CI/CD, GitHub Action, paid
  service or additional deployment resource was added.
- All changes remain unstaged and uncommitted.

## Exact next item

Step 10.7 is the backend/ML acceptance gate. Before it can be declared passed,
the user-controlled review, commit and push must make `f0010_step_10_5`
available. Production rollout must then be coordinated in this order: apply
the forward migration with the direct owner credential, verify the read-only
`pl_api` role and schema head, manually deploy the same commit to Render and
repeat the public acceptance checks. Do not migrate Neon ahead of the currently
deployed code, because commit `461f31f` correctly expects `f0009_step_7_9`.
