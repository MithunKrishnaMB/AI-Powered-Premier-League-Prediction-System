[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[project]
name = "premier-league-prediction-platform"
version = "0.1.0"
description = "Backend-first Premier League prediction, simulation and analytics platform"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "alembic==1.19.2",
    "catboost==1.2.10",
    "fastapi==0.141.1",
    "numpy==2.5.3",
    "psycopg[binary]==3.3.5",
    "pydantic==2.13.5",
    "pydantic-settings==2.15.0",
    "SQLAlchemy==2.0.52",
    "starlette==1.6.0",
    "tzdata==2026.3",
]

[project.scripts]
plp-download-historical = "pl_platform.ingestion.download:main"
plp-materialize-historical = "pl_platform.ingestion.materialize:main"
plp-materialize-features = "pl_platform.features.materialize:main"
plp-materialize-training = "pl_platform.training.materialize:main"
plp-evaluate-models = "pl_platform.evaluation.materialize:main"
plp-tune-catboost = "pl_platform.evaluation.catboost_materialize:main"
plp-evaluate-advanced-models = "pl_platform.evaluation.advanced_materialize:main"
plp-assess-models = "pl_platform.evaluation.assessment_materialize:main"
plp-build-model-artifact = "pl_platform.registry.serialization:main"
plp-register-model-artifact = "pl_platform.registry.registry:main"
plp-check-database = "pl_platform.persistence.database:main"

[dependency-groups]
dev = [
    "httpx2==2.13.0",
    "mypy==2.3.1",
    "pytest==9.1.1",
    "pytest-cov==7.1.0",
    "ruff==0.16.6",
]

[tool.pytest.ini_options]
addopts = [
    "--strict-config",
    "--strict-markers",
    "-ra",
]
markers = [
    "postgresql: requires configured local PostgreSQL development and test databases",
]
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
pl_platform = ["py.typed"]

[tool.coverage.run]
branch = true
source = ["pl_platform"]

[tool.coverage.report]
exclude_also = [
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
]
show_missing = true
skip_covered = true
fail_under = 90

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = [
    "B",
    "E",
    "F",
    "I",
    "RUF",
    "SIM",
    "UP",
]

[tool.ruff.lint.isort]
known-first-party = ["pl_platform"]

[tool.ruff.format]
docstring-code-format = true
line-ending = "lf"

[tool.mypy]
python_version = "3.14"
strict = true
pretty = true
show_error_codes = true
warn_unreachable = true
